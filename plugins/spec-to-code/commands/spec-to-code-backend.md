---
description: Build a backend/API/DB feature from a spec (gated TDD; integration/contract/DB tests).
argument-hint: "[spec file/path/paste]"
---
Run the **spec-to-code-backend** skill on the spec below (its SKILL.md is authoritative). Honor '문서까지만' (docs scope), '꼼꼼히' (step-through), '가볍게' (lite).

Spec / target: $ARGUMENTS
