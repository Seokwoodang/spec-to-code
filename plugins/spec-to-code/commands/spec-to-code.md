---
description: Alias of /spec-to-code-frontend (frontend/UI build). For backend use /spec-to-code-backend, for both use /spec-to-code-fullstack.
argument-hint: "[spec file/path/paste]"
---
**`/spec-to-code` is an alias for the frontend specialist.** Run the **spec-to-code-frontend** skill on the spec below (its SKILL.md is authoritative). If the request is clearly server/API/DB-only, suggest `/spec-to-code-backend`; if it spans UI + server, suggest `/spec-to-code-fullstack`.

Spec / target: $ARGUMENTS
