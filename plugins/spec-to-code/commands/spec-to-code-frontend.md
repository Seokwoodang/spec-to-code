---
description: Build a frontend/UI feature from a spec (gated TDD; Playwright/screenshots). Add '문서까지만' for docs-only.
argument-hint: "[spec file/path/paste]"
---
Run the **spec-to-code-frontend** skill on the spec below (its SKILL.md is authoritative). Honor options said in the prompt: '문서까지만/구현 하지마' → docs scope (stop after Phase 6); '꼼꼼히' → step-through; '가볍게' → lite.

Spec / target: $ARGUMENTS
